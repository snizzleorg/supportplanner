# Copy this to .env in the project root

# --- Authentik OIDC ---
OIDC_ISSUER_URL=https://auth.example.com/application/o/supportplanner/
OIDC_CLIENT_ID=replace-with-client-id
OIDC_CLIENT_SECRET=replace-with-client-secret
OIDC_REDIRECT_URI=http://localhost:5175/auth/callback
OIDC_SCOPES=openid profile email
OIDC_POST_LOGOUT_REDIRECT_URI=http://localhost:5175/logged-out

# --- App ---
PORT=5173
NEXTCLOUD_URL=https://your-nextcloud
NEXTCLOUD_USERNAME=your-username
NEXTCLOUD_PASSWORD=your-password

# --- Roles (optional) ---
# Map IdP groups to roles (comma-separated names). If your IdP does not send groups, leave these empty.
# ADMIN_GROUPS=
# EDITOR_GROUPS=
# Map roles by email (comma-separated). Emails are matched case-insensitively and override groups mapping.
# ADMIN_EMAILS=
# EDITOR_EMAILS=
