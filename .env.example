# Environment variables for SupportPlanner
# Copy this file to .env and fill in the values. Do NOT commit .env

NEXTCLOUD_URL=
NEXTCLOUD_USERNAME=
NEXTCLOUD_PASSWORD=
PORT=5173

# Optional: Role mapping and OIDC logout
# Comma-separated lists of IdP groups to map to roles
# ADMIN_GROUPS=
# EDITOR_GROUPS=
# Comma-separated lists of emails to map to roles (overrides groups)
# ADMIN_EMAILS=
# EDITOR_EMAILS=
# Optional: RP-initiated logout redirect URI (must be allowed at the IdP)
# OIDC_POST_LOGOUT_REDIRECT_URI=
